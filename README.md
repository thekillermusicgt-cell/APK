# Flujo actualizado

Se añadió `index-con-silencios.html`, una versión de SRTSync donde **Quitar silencios aparece arriba de todo**. El audio procesado se usa automáticamente en la sincronización y permite descargar el `.srt`.

Para usarlo, abre este archivo desde la carpeta descargada. La aplicación original `index.html` no se modifica.
